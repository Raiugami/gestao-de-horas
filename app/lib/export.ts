import {type Person,type JiraDayAdjustment,summary,dayResult,hours,monthDays,weekDay,escapeHtml as e,csvCell,displayName,normalize} from './audit';
import {type JiraCategory,type JiraPerson,findJiraPerson,jiraCategoriesForDay} from './jira';
const reportStyles=`:root{color-scheme:light;--page-bg:#fff;--surface:#fff;--surface-alt:#f7f6f3;--ink:#260717;--heading:#480e2a;--muted:#646459;--border:#dedbd5;--notice-bg:#fff3e2;--notice-border:#d99239;--notice-ink:#794617;--bar-bg:#f1f1ed;--input-bg:#fff;--good:#17684d;--warn:#a44209;--bad:#b42335;--blue:#1f5f91;--day-ok-bg:#e7f2ea;--day-ok-fg:#17684d;--day-justified-bg:#fff3bf;--day-justified-fg:#785b00;--day-justified-border:#e3c45c;--day-vacation-bg:#e2f0fb;--day-vacation-fg:#1f5f91;--day-vacation-border:#9bc4e5;--day-pending-bg:#fff0e5;--day-pending-fg:#a04912;--day-review-bg:#f3eaf0;--day-review-fg:#713450;--day-excluded-bg:#f1f1ed;--day-excluded-fg:#646459}:root[data-theme="dark"]{color-scheme:dark;--page-bg:#151119;--surface:#211b25;--surface-alt:#2b2430;--ink:#f2edf1;--heading:#f5c8dc;--muted:#b9adb7;--border:#493b4a;--notice-bg:#352a20;--notice-border:#a87943;--notice-ink:#f0ce9f;--bar-bg:#39313c;--input-bg:#211b25;--good:#75d2a1;--warn:#f3bd70;--bad:#ff929a;--blue:#8dcaff;--day-ok-bg:#173529;--day-ok-fg:#8ce0b2;--day-justified-bg:#493a17;--day-justified-fg:#f8d66d;--day-justified-border:#91752d;--day-vacation-bg:#15344a;--day-vacation-fg:#a2d8ff;--day-vacation-border:#39759e;--day-pending-bg:#482c25;--day-pending-fg:#ffb79f;--day-review-bg:#462b3b;--day-review-fg:#e8afcb;--day-excluded-bg:#322d35;--day-excluded-fg:#c4bbc5}@media(prefers-color-scheme:dark){:root:not([data-theme]){color-scheme:dark;--page-bg:#151119;--surface:#211b25;--surface-alt:#2b2430;--ink:#f2edf1;--heading:#f5c8dc;--muted:#b9adb7;--border:#493b4a;--notice-bg:#352a20;--notice-border:#a87943;--notice-ink:#f0ce9f;--bar-bg:#39313c;--input-bg:#211b25;--good:#75d2a1;--warn:#f3bd70;--bad:#ff929a;--blue:#8dcaff;--day-ok-bg:#173529;--day-ok-fg:#8ce0b2;--day-justified-bg:#493a17;--day-justified-fg:#f8d66d;--day-justified-border:#91752d;--day-vacation-bg:#15344a;--day-vacation-fg:#a2d8ff;--day-vacation-border:#39759e;--day-pending-bg:#482c25;--day-pending-fg:#ffb79f;--day-review-bg:#462b3b;--day-review-fg:#e8afcb;--day-excluded-bg:#322d35;--day-excluded-fg:#c4bbc5}}*{box-sizing:border-box}body{font:15px/1.5 'Segoe UI',Arial,sans-serif;color:var(--ink);max-width:1500px;margin:32px auto;padding:0 4%;background:var(--page-bg);transition:background-color .2s,color .2s}h1{font-size:30px;font-weight:500;color:var(--heading);border-top:5px solid #ff0054;padding-top:18px;margin:16px 0 4px}h2{margin:30px 0 10px;font-size:20px;font-weight:500;color:var(--heading)}.muted{color:var(--muted)}.notice{padding:13px 16px;background:var(--notice-bg);color:var(--notice-ink);border-left:4px solid var(--notice-border);border-radius:4px}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin:24px 0}.card{border:1px solid var(--border);border-radius:8px;padding:16px;background:var(--surface)}.card b{display:block;font-size:26px;line-height:1.2;margin:5px 0}.card small{color:var(--muted)}.good{color:var(--good)}.warn{color:var(--warn)}.bad{color:var(--bad)}.blue{color:var(--blue)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}.panel{border:1px solid var(--border);border-radius:8px;padding:18px;background:var(--surface)}.bar{height:9px;background:var(--bar-bg);border-radius:8px;overflow:hidden;margin:7px 0 12px}.bar i{display:block;height:100%;border-radius:8px}table{width:100%;border-collapse:collapse;font-size:13px;margin:12px 0}td,th{border-bottom:1px solid var(--border);padding:9px;text-align:left}th{background:var(--surface-alt);color:var(--muted);font-weight:600}.status{font-weight:600}.report-controls{display:flex;gap:10px;margin:12px 0}.report-controls input,.report-controls select{border:1px solid var(--border);border-radius:6px;padding:9px 10px;font:inherit;color:var(--ink);background:var(--input-bg)}.report-controls input{flex:1}.daily-panel{overflow:auto}.daily-table{min-width:900px;font-size:11px}.daily-table th,.daily-table td{padding:5px;text-align:center;white-space:nowrap}.daily-table th:first-child,.daily-table td:first-child{text-align:left}.day-cell{display:inline-block;min-width:29px;padding:5px 2px;border-radius:2px;font-weight:600;font-variant-numeric:tabular-nums}.day-cell.ok{background:var(--day-ok-bg);color:var(--day-ok-fg)}.day-cell.justified{background:var(--day-justified-bg);color:var(--day-justified-fg);border:1px solid var(--day-justified-border)}.day-cell.vacation{background:var(--day-vacation-bg);color:var(--day-vacation-fg);border:1px solid var(--day-vacation-border)}.day-cell.pending{background:var(--day-pending-bg);color:var(--day-pending-fg)}.day-cell.review{background:var(--day-review-bg);color:var(--day-review-fg)}.day-cell.excluded{background:var(--day-excluded-bg);color:var(--day-excluded-fg);border:1px solid var(--border)}.daily-table th:first-child{min-width:130px}.daily-legend{margin-top:10px}.pending{color:var(--warn)}.review{color:#c984a2}.ok{color:var(--good)}.no-print{display:flex;justify-content:flex-end;gap:10px;margin-bottom:14px}button{background:#480e2a;color:#fff;border:1px solid #480e2a;border-radius:6px;padding:10px 18px;cursor:pointer;font:inherit}button:hover{filter:brightness(1.12)}button:focus-visible{outline:3px solid #ff0054;outline-offset:3px}.button-secondary{background:var(--surface);color:var(--ink);border-color:var(--border)}.legend{display:flex;gap:16px;flex-wrap:wrap;color:var(--muted);font-size:13px}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:5px}@media(max-width:800px){body{margin:18px auto}.cards{grid-template-columns:repeat(2,1fr)}.grid{grid-template-columns:1fr}.no-print{justify-content:stretch}.no-print button{flex:1}}@page{size:A4 landscape;margin:12mm}@media print{:root,:root[data-theme="dark"]{color-scheme:light;--page-bg:#fff;--surface:#fff;--surface-alt:#f7f6f3;--ink:#260717;--heading:#480e2a;--muted:#646459;--border:#dedbd5;--notice-bg:#fff3e2;--notice-border:#d99239;--notice-ink:#794617;--bar-bg:#f1f1ed;--input-bg:#fff;--good:#17684d;--warn:#a44209;--bad:#b42335;--blue:#1f5f91;--day-ok-bg:#e7f2ea;--day-ok-fg:#17684d;--day-justified-bg:#fff3bf;--day-justified-fg:#785b00;--day-justified-border:#e3c45c;--day-vacation-bg:#e2f0fb;--day-vacation-fg:#1f5f91;--day-vacation-border:#9bc4e5;--day-pending-bg:#fff0e5;--day-pending-fg:#a04912;--day-review-bg:#f3eaf0;--day-review-fg:#713450;--day-excluded-bg:#f1f1ed;--day-excluded-fg:#646459}body{margin:0;padding:0;background:#fff;color:#111}.no-print{display:none}.cards{grid-template-columns:repeat(4,1fr)}button{display:none}.card,.panel{background:#fff;color:#260717}.report-controls input,.report-controls select{background:#fff;color:#260717}.muted,th{color:#646459}}`;
export function saveFile(text:string,name:string,type:string){const url=URL.createObjectURL(new Blob([text],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.style.display='none';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),5000);}
export function exportCsv(people:Person[],month:string,holidays:Record<string,string>,confirmed:boolean){
 const rows:unknown[][]=[['Colaborador','Arquivo','Data','Horas identificadas (decimais)','Horas esperadas','Resultado','Motivo','Ajuste de leitura','Confirmação OCR','Período confirmado']];
 for(const p of people)for(const d of p.days){const r=dayResult(p,d,month,holidays);rows.push([displayName(p.name),p.fileName,`${String(d.day).padStart(2,'0')}/${month.slice(5)}/${month.slice(0,4)}`,r.cents===null?'':hours(r.cents),hours(r.expected),r.status==='ok'?'OK':r.status==='review'?'Leitura a revisar':r.status==='excluded'?'Dispensado':'Divergência',r.reason,r.correction?.reason??'',p.ocr?(p.ocrReviewed?'Confirmado':'Revisão necessária'):'Texto do PDF',confirmed?'Sim':'Não']);}
 saveFile('\uFEFF'+rows.map(r=>r.map(csvCell).join(';')).join('\r\n'),`conferencia-${month}.csv`,'text/csv;charset=utf-8');
}
export function reportHtml(people:Person[],month:string,holidays:Record<string,string>,confirmed:boolean,notes:string[]=[],jiraAdjustments:Record<string,Record<string,JiraDayAdjustment>>={},jiraPeople:JiraPerson[]=[]){
 const generated=new Date().toLocaleString('pt-BR');const stats=people.map(person=>({person,summary:summary(person,month,holidays,jiraAdjustments[person.id]??{})}));const pending=stats.filter(x=>x.summary.status==='pending');const review=stats.filter(x=>x.summary.status==='review');const complete=stats.filter(x=>x.summary.status==='ok');const missing=stats.reduce((sum,x)=>sum+x.summary.missing,0);const excess=stats.reduce((sum,x)=>sum+x.summary.excess,0);const hasCategory=(person:JiraPerson,category:JiraCategory)=>{const days=new Set([...Object.keys(person.days),...Object.keys(person.justifications??{}),...Object.keys(person.justificationCategories??{}),...(person.issueDetails??[]).flatMap(issue=>Object.keys(issue.days))]);return Array.from(days).some(day=>jiraCategoriesForDay(person,day).includes(category));};const personKey=(value:string)=>normalize(displayName(value)).replace(/[^A-Z0-9]+/g,' ').split(' ').filter(Boolean).sort().join(' ');const jiraFor=(person:Person)=>findJiraPerson(person,jiraPeople);const countPeople=(category:JiraCategory)=>new Set(people.flatMap(person=>{const jira=jiraFor(person);return jira&&hasCategory(jira,category)?[personKey(person.name)]:[]})).size;const withAbsence=countPeople('atestado');const withVacation=countPeople('ferias');const pct=(value:number)=>people.length?Math.round(value/people.length*100):0;const bar=(value:number,color:string)=>`<div class="bar"><i style="width:${pct(value)}%;background:${color}"></i></div>`;
 const head=`<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Relatório executivo · Gestão de horas · ${e(month)}</title><style>${reportStyles}</style><div class="no-print"><button type="button" id="theme-toggle" class="button-secondary" aria-pressed="false">Ativar modo escuro</button><button type="button" onclick="window.print()">Imprimir / salvar como PDF</button></div><h1>Relatório executivo · Gestão de horas</h1><p>Referência: <b>${e(month.slice(5)+'/'+month.slice(0,4))}</b> · Emitido em ${e(generated)}</p>${!confirmed?'<p class="notice">PRELIMINAR: confirme o mês de referência antes do fechamento.</p>':''}${review.length?'<p class="notice">Há leituras de imagem ou formatos que ainda exigem confirmação.</p>':''}`;
 const cards=`<div class="cards"><div class="card"><small>Conformidade</small><b class="good">${pct(complete.length)}%</b><small>${complete.length} de ${people.length} pessoas</small></div><div class="card"><small>Pendências reais</small><b class="warn">${pending.length}</b><small>pessoas</small></div><div class="card"><small>Déficit / excesso</small><b class="bad">${hours(missing)} h</b><small>${hours(excess)} h de excesso</small></div><div class="card"><small>Leitura a revisar</small><b class="review">${review.length}</b><small>pessoas</small></div></div>`;
 const charts=`<div class="grid"><section class="panel"><h2>Distribuição da equipe</h2><p>Conformes · ${complete.length} (${pct(complete.length)}%)</p>${bar(complete.length,'#258458')}<p>Pendências · ${pending.length} (${pct(pending.length)}%)</p>${bar(pending.length,'#c37224')}<p>Leituras a revisar · ${review.length} (${pct(review.length)}%)</p>${bar(review.length,'#866370')}</section><section class="panel"><h2>Cobertura pelo Jira</h2><p class="blue">Férias · ${withVacation} pessoa(s)</p>${bar(withVacation,'#3b82b6')}<p class="warn">Atestados · ${withAbsence} pessoa(s)</p>${bar(withAbsence,'#d4ad32')}<p class="muted">O Jira é usado como evidência complementar; a confirmação final permanece humana.</p></section></div>`;
 const days=Array.from({length:monthDays(month)},(_,index)=>index+1);const weekdayLabels=['D','S','T','Q','Q','S','S'];const dailyMap=`<h2>Mapa diário</h2><div class="report-controls"><input id="report-search" placeholder="Buscar colaborador..." aria-label="Buscar colaborador"><select id="report-filter" aria-label="Filtrar situação"><option value="all">Todas as situações</option><option value="pending">Com divergências</option><option value="review">Leitura a revisar</option><option value="ok">Sem divergências</option><option value="jira">Atestados e férias</option></select></div><section class="panel daily-panel"><table class="daily-table"><thead><tr><th>Colaborador</th>${days.map(day=>`<th>${String(day).padStart(2,'0')}<br><span class="muted">${weekdayLabels[weekDay(month,day)]}</span></th>`).join('')}</tr></thead><tbody>${people.map(person=>`<tr data-name="${e(displayName(person.name)).toLowerCase()}" data-status="${summary(person,month,holidays,jiraAdjustments[person.id]??{}).status}" data-jira="${Object.keys(jiraAdjustments[person.id]??{}).length>0?'yes':'no'}"><td>${e(displayName(person.name))}</td>${days.map(day=>{const source=person.days.find(item=>item.day===day);if(!source)return '<td>—</td>';const result=dayResult(person,source,month,holidays);const adjustment=jiraAdjustments[person.id]?.[String(day)];const covered=Boolean(adjustment&&result.cents!==null&&result.cents+adjustment.hours===adjustment.jiraHours&&result.status==='pending');const value=covered?adjustment!.jiraHours:result.cents;const jiraPerson=jiraFor(person);const categories=jiraCategoriesForDay(jiraPerson,String(day));const vacation=Boolean(source.vacation)||categories.includes('ferias')&&!categories.includes('atestado');const kind=source.vacation?'vacation':adjustment&&adjustment.hours>0?(vacation?'vacation':'justified'):result.status;return `<td><span class="day-cell ${kind}" title="${e(result.reason)}">${value===null?'?':result.status==='excluded'&&value===0?'—':hours(value)}</span></td>`;}).join('')}</tr>`).join('')}</tbody></table><div class="legend daily-legend"><span><i class="dot" style="background:#258458"></i>Conforme</span><span><i class="dot" style="background:#d4ad32"></i>Atestado no Jira</span><span><i class="dot" style="background:#3b82b6"></i>Férias no PDF ou Jira</span><span><i class="dot" style="background:#b42335"></i>Abaixo de 8 h</span><span><i class="dot" style="background:#866370"></i>Leitura a revisar</span><span><i class="dot" style="background:#a8a8a0"></i>Dispensado</span></div></section>`; const pendingTable=`<h2>Ações prioritárias</h2>${pending.length?`<table><thead><tr><th>Colaborador</th><th>Dias pendentes</th><th>Déficit</th><th>Excesso</th></tr></thead><tbody>${pending.map(item=>`<tr><td>${e(displayName(item.person.name))}</td><td>${item.summary.pending}</td><td class="bad">${hours(item.summary.missing)} h</td><td>${hours(item.summary.excess)} h</td></tr>`).join('')}</tbody></table>`:'<p class="panel good">Nenhuma pendência real após a reconciliação com o Jira.</p>'}`;
 const notesBlock=notes.length?'<h2>Observações</h2>'+notes.map(note=>'<p class="notice">'+e(note)+'</p>').join(''):'';
 const reportScript=`<script>(function(){const themeToggle=document.getElementById("theme-toggle"),root=document.documentElement,systemDark=window.matchMedia?window.matchMedia("(prefers-color-scheme: dark)").matches:false;function setTheme(dark){root.dataset.theme=dark?"dark":"light";themeToggle.setAttribute("aria-pressed",String(dark));themeToggle.textContent=dark?"Ativar modo claro":"Ativar modo escuro";}setTheme(systemDark);themeToggle.addEventListener("click",()=>setTheme(root.dataset.theme!=="dark"));const search=document.getElementById('report-search'),filter=document.getElementById('report-filter'),rows=Array.from(document.querySelectorAll('.daily-table tbody tr'));function update(){const query=(search.value||'').toLocaleLowerCase('pt-BR').trim();const selected=filter.value;rows.forEach(row=>{const matchesName=!query||row.dataset.name.includes(query);const matchesFilter=selected==='all'||(selected==='jira'&&row.dataset.jira==='yes')||row.dataset.status===selected;row.hidden=!(matchesName&&matchesFilter);});}search.addEventListener('input',update);filter.addEventListener('change',update);})();</script>`; return head+cards+charts+dailyMap+pendingTable+notesBlock+reportScript+'<p class="muted">Regra: 8 horas por dia útil. Valores em horas decimais. Este relatório é executivo e não substitui a consulta aos detalhes na aplicação.</p></html>';
}
